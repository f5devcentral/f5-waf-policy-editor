import React from "react";
import { Button } from "@mui/material";

export const StartConvertPage: React.VoidFunctionComponent = () => {
  return (
    <div>
      <Button variant={"contained"}>Start Convert</Button>
    </div>
  );
};
